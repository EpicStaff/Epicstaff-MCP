{
  "mcpServers": {
    "epicstaff": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/index.js"],
      "env": {
        "ES_URL": "${ES_URL}",
        "ES_EMAIL": "${ES_EMAIL}",
        "ES_PASSWORD": "${ES_PASSWORD}"
      }
    }
  }
}
