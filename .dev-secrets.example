# Copy to .dev-secrets (gitignored) and fill in. Sourced by run-dev.sh.
# The admin-owned API key minted via SuperadminBootstrap on the dev backend.
# Re-mint after `make clean` resets the dev DB (see ONBOARDING / memory).
export EPICSTAFF_API_KEY=replace-with-admin-key
