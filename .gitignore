node_modules/
*.log
.DS_Store
coverage/
# dist/ is committed on release builds (the plugin launches dist/index.js),
# but intermediate local builds shouldn't pollute diffs — commit dist/ deliberately.
